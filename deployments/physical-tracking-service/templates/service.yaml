apiVersion: v1
kind: Service
metadata:
  name: {{ include "physical-tracking-service.fullname" . }}
  labels:
    {{- include "physical-tracking-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "physical-tracking-service.selectorLabels" . | nindent 4 }}
